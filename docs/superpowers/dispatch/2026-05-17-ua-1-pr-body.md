# U-A1 PR — manual creation (gh not installed)

Paste the URL into a browser, fill Title + Body from below, click Create.

## URL

<https://github.com/takaki-maeda-99/MimicAnno/pull/new/feat/ua-1-catalog-jobs>

Base: `main` / Compare: `feat/ua-1-catalog-jobs` (commit `5e3bda9`)

## Title

```
feat(ua-1): catalog + job-kick backend and frontend
```

## Body

```markdown
## Summary

Implements U-A1 of the U-A initiative — dataset catalog API + annotate-job kick / monitoring API + frontend `/datasets` and `/jobs` pages — per master spec `docs/superpowers/specs/2026-05-17-ua-dataset-processing-ui-design.md` §3.1.

- **Backend**: `GET /api/datasets`, `GET /api/datasets/{name}`, `POST /api/jobs`, `GET /api/jobs[?status=]`, `GET /api/jobs/{id}`, `GET /api/jobs/{id}/log`, `GET /api/jobs/{id}/stream` (SSE), `DELETE /api/jobs/{id}`. CORS allow_methods extended with POST + DELETE.
- **Job runner**: per-GPU FIFO queue, subprocess wrapping `scripts/batch_annotate_4B.py` / `mimicanno annotate` with stdout marker `[mimicanno-job-progress] ep=<idx> finished=<k>/<total>`. `.mimicanno-jobs/` at `runs_root.parent`, override via `--jobs-dir`. Restart reclassifies dead-PID `running` jobs to `failed`.
- **Frontend**: `DatasetsPage` (catalog + per-ep status + Annotate modal) and `JobsPage` (list + live log tail via SSE).

Touches master §2 contract: no

## Test plan

- [x] Backend: 53 new tests across B1–B10 (catalog, jobs CRUD, job store, runner with mocked subprocess, progress marker, SSE, restart reclassification, CORS preflight) — all pass. Total: 290 pass, 0 fail (was 237).
- [x] Frontend: 13 new vitest cases for `DatasetsPage` / `JobsPage` / annotate modal — all pass. Total: 136 pass (was 123).
- [x] `uv run mypy --strict mimicanno/server/` — clean (0 errors in 18 files).
- [x] Baseline regression: existing tests untouched and still pass.

## Known follow-ups

- `App.tsx` routing not wired — `DatasetsPage` / `JobsPage` components exist but the existing URL-param-based router (`?run=...`) doesn't surface them yet. Decision on routing strategy left to follow-up.
- `scripts/batch_annotate_4B.py` accepts dataset by hardcoded config; the runner's `_build_command` passes `--dataset` but doesn't yet forward arbitrary `robot_config` / `pipeline_config`. Wrap-as-is per spec §3.1 intent.
- FastAPI `@app.on_event("startup")` deprecation warning — matches existing codebase pattern.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

## After PR is created

Reply with the PR number to the commander session so TODO U-A1 status column can be updated from `dispatched(...)` to `PR#<n>`.
