# U-A4 PR — manual creation (gh not installed)

Paste the URL into a browser, fill Title + Body from below.

## URL

<https://github.com/takaki-maeda-99/MimicAnno/pull/new/feat/ua-4-mask-overlay>

Base: `main` / Compare: `feat/ua-4-mask-overlay` (commit `5c398f9`)

## Title

```
feat(ua-4): SAM3 mask overlay — sidecar writer + backend routes + canvas UI
```

## Body

```markdown
## Summary

Implements U-A4 of the U-A initiative — annotate-time pre-baked mask PNG sidecar + read-side HTTP routes + canvas overlay UI — per master spec rev3 §3.4 + §2.5 (`docs/superpowers/specs/2026-05-17-ua-dataset-processing-ui-design.md` @ `eb389ba`).

- **Pipeline amendment**: `write_masks_sidecar()` persists `MaskCache.by_frame` as `_masks/<frame:06d>.png` (RGBA) + `_masks/meta.json` inside `_write_artifacts()` in `pipeline.py`. Only runs when `mask_cache is not None` (mask_overlay_enabled=True). Writes go through the atomic-publish flow.
- **Backend routes** (`mimicanno/server/mask_routes.py`): `GET /api/runs/{canonical}/masks/meta.json` and `GET /api/runs/{canonical}/masks/{frame}` registered BEFORE the catch-all `{artifact}` route per master §2.5. Legacy runs → 200 frame_count=0 / 204 respectively (graceful degradation, no backfill required at merge).
- **Frontend**: `MaskOverlay.tsx` + `masksClient.ts` — RGBA PNG fetched per-frame (100 ms debounce), drawn on absolutely-positioned canvas inside `VideoPlayer.tsx`. Alpha slider + per-track color swatch + per-track visibility toggle. `VideoPlayer.tsx` extended with `maskOverlay?: ReactNode` slot prop (only canvas overlay added, video element untouched).

Touches master §2 contract: no

## Test plan

- [x] `uv run pytest tests/test_masks_sidecar.py tests/server/test_mask_routes.py -v` — 20/20 pass (8 sidecar + 12 route)
- [x] `uv run mypy --strict mimicanno/masks/ mimicanno/server/mask_routes.py` — 0 errors
- [x] `cd frontend && npm test` — 161/161 pass (+14 new: 5 masksClient, 7 MaskOverlay, 2 VideoPlayer integration)
- [ ] Real-data smoke: annotate with `mask_overlay.enabled=true` on SO101 ep0, inspect `_masks/` sidecar contents and the overlay rendering

## Known follow-ups

- Backfill CLI for legacy runs without `_masks/` (master rev3 §5: `U-A4-followup-backfill`) — deferred. Legacy runs degrade gracefully via 204.
- Performance for high-fps scrubbing (per-frame PNG fetch) — 100 ms debounce in place; if scrubbing is slow on large episodes, consider client-side cache or sprite-sheet packing as a follow-up.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

## After PR is created

Reply with the PR number to the commander session so TODO U-A4 status column can be updated to `PR#<n>` and then `merged` after review/merge.
