# U-A5 progress badge PR — manual creation

## URL

<https://github.com/takaki-maeda-99/MimicAnno/pull/new/feat/ua-5-progress-badge>

Base: `main` / Compare: `feat/ua-5-progress-badge` (commit `3455263`)

## Title

```
feat(ua-5): site-wide running jobs badge
```

## Body

```markdown
## Summary

Implements U-A5 of the U-A initiative — small header component polling running-jobs count. Per master spec rev3 §3.5 + §2.3 (consumes U-A1's `/api/jobs?status=running` endpoint).

- Adds `JobsBadge` component that polls `GET /api/jobs?status=running` every 4 s and shows a "N running" pill badge in the app header linking to `?page=jobs`. Renders nothing when count is 0 (fail-silent on errors).
- Adds `jobsBadgeClient.ts` with `fetchRunningCount()` — wraps the U-A1 jobs endpoint, returns 0 on any error.
- Extends `App.tsx` with `?page=datasets` / `?page=jobs` routing (DatasetsPage and JobsPage now reachable via URL) and a persistent header bar mounting `<JobsBadge />`.

Touches master §2 contract: no

## Test plan

- [x] `jobsBadgeClient.test.ts` — 3 cases: success count, network error → 0, non-ok HTTP → 0
- [x] `JobsBadge.test.tsx` — 5 cases: hidden when count=0, shows "N running" when count>0, fail-silent on error, href=?page=jobs
- [x] `App.badge.test.tsx` — 3 cases: header present on all pages, ?page=datasets routing, default RunList route
- [x] `cd frontend && npm test` — 159 total pass (+11 new, 0 regressions)
- [ ] Manual smoke: run a job, observe badge appears with count; cancel job, badge hides

## Files changed

- NEW: `frontend/src/lib/jobsBadgeClient.ts`
- NEW: `frontend/src/components/JobsBadge.tsx`
- NEW: 3 test files (client + component + app integration)
- MODIFIED: `frontend/src/App.tsx` — added `?page=` routing + header with badge

## Known

- Polling interval is 4 s (within the 3–5 s spec window).
- `frontend/node_modules/` was installed via `npm install` in the agent's isolated worktree (not committed).
- The `?page=` routing here overlaps with U-A1 follow-up routing PR (`feat/ua-1-followup-routing`). If both PRs are open, the second to merge will hit a small App.tsx conflict — resolve by taking the merged superset (datasets + jobs + header badge).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

## After PR is created

Reply with the PR number to commander.
