# U-A3 PR body (copy-paste into GitHub web UI)

**URL**: https://github.com/takaki-maeda-99/MimicAnno/pull/new/feat/ua-3-vlm-panel

**Title**: `feat(ua-3): VLM dumps viewer (right-panel) + master §2.4 rev3`

**Body** (copy below the line):

---

## Summary

Implements U-A3 end-to-end per master spec §2.4 rev3 + §3.3. The first commit (`3f484ad`) rewrites §2.4 itself — that was authorized by the commander during this dispatch session because the original spec assumed a `*.jsonl` layout that doesn't exist on disk anywhere.

- Backend: `GET /api/runs/{canonical}/vlm_dumps.json?run_set=<rs>`, registered before the `/api/runs/{name}/{artifact}` catch-all
- Backend reader (`mimicanno/server/vlm_dumps.py`): walks `runs/<rs>/_vlm_dumps/<episode_id>/{_planner,s_NNN}/...`, picks highest `attempt_M` per segment, marks `failed=true` for segment calls whose `response.txt` is missing or non-JSON
- Frontend: `VlmPanel` mounted in the RunViewer right slot (`VlmPanelHost` wrapper memoizes canonical + selectedSegmentId so VlmPanel doesn't re-render on every playback tick); planner section + segment section; rows expand on click; row whose `segment_id` matches `selectSegmentIdByTime(segments, currentTimeSec)` gets `data-selected="true"`
- Master spec §2.4 rev3 (commit `3f484ad`): documents the actual on-disk tree-of-files layout, run-set-scoped path, `kind: "planner" | "segment"` field, nullable `ms` / `model_variant`, image bytes deferred

## Touches master §2 contract

**Yes** — §2.4 was rewritten in commit `3f484ad`. Commander-approved during dispatch after on-disk inspection found the original spec was factually incorrect on three points:

| Aspect | Old §2.4 (rev2) | New §2.4 (rev3, actual) |
|---|---|---|
| Format | `_vlm_dumps/*.jsonl` flat | directory tree (`prompt.txt`, `response.txt`, `request.json`, `*.png`) |
| Location | `runs/<rs>/<canonical>/_vlm_dumps/` | `runs/<rs>/_vlm_dumps/` (run-set-scoped) |
| Key | canonical name | source `episode_id` (resolved via `index.json`) |

Verified against `so101_phase4_v5`, `g3_smoke_*`, `gem4_*`, `piper_phase4_v5` — zero `.jsonl` files found anywhere in any `_vlm_dumps/`.

Other sub-projects (U-A1 / U-A2 / U-A4 / U-A5) are unaffected: §2.4 was self-contained.

## Test plan

- [x] Backend: `uv run pytest tests/server/ -v` → **254 passed** (was 237 baseline; +17 new)
- [x] mypy: `uv run mypy --strict mimicanno/server/` → clean
- [x] Frontend: `cd frontend && npm test` → **135 passed** (was 123 baseline; +12 new)
- [x] No touches to U-A4 territory (`VideoPlayer.tsx`, masks routes), U-A1 (jobs / catalog), or other sub-Claude scope
- [x] Self code review (general-purpose subagent) pass: 1 Important + 4 Minor → all applied in commit `b918c3e`
  - Important: `run_set=".."` path-traversal guard (could read `index.json` one level above `runs_root`); regression test added
  - Minor: registration-order test assertions strengthened, `attempt_M < 0` rejected, `useMemo` wrapper for re-render hygiene

## Commits

1. `3f484ad` docs(ua-spec): §2.4 rev3 — vlm_dumps actual layout (commander-approved)
2. `214c792` docs(ua-3): sub-spec + plan
3. `39b5477` feat(ua-3): vlm_dumps reader module + 10 tests
4. `f4349c6` feat(ua-3): GET /api/runs/{canonical}/vlm_dumps.json route + 6 tests
5. `b05d622` feat(ua-3): VlmPanel + vlmClient + 9 vitest cases
6. `78e8de0` feat(ua-3): wire VlmPanel into RunViewer right slot + 3 vitest cases
7. `b918c3e` fix(ua-3): apply code-review fixes (path-traversal guard + test strengthening + memo)
8. `79a3bd3` docs(todo): U-A3 branch pushed + entry in 未マージ PR 待ち

## Follow-ups (not in this PR)

- Image bytes endpoint for keyframe / planner frame display (text-only in rev3)
- Writer-side `ms` + `model_variant` logging — would populate currently-nullable fields
- Pre-existing `get_effective_root` path-traversal pattern: `Path("..").name == ".."` slips past `.name` check on the OTHER routes too (PATCH boundary / labels / reviewed; GET artifact catch-all). Not fixed here to keep scope tight, but worth a follow-up hardening PR — same guard pattern as `routes.py:608` in this PR.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
